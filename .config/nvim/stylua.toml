column_width = 120
line_endings = "Unix"               # Unix / Windows
indent_type = "Spaces"              # Spaces / Tabs
indent_width = 2
quote_style = "AutoPreferSingle"    # AutoPreferDouble / ForceDouble / AutoPreferSingle / ForceSingle / AutoPrefer
call_parentheses = "Always"         # Always / NoSingleString / NoSingleTable / None / Input
collapse_simple_statement = "Never" # Never / FunctionOnly / ConditionalOnly / Always

[sort_requires]
enabled = false
